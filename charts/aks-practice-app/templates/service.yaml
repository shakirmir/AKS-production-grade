apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}
    version: {{ .Values.service.selectorVersion }}
  ports:
    - port: 80
      targetPort: 8080
